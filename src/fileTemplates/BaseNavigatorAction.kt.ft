#if (${PACKAGE_NAME} && ${PACKAGE_NAME} != "")
package ${PACKAGE_NAME}
#end

abstract class NavigatorAction<out N>(protected val navigator: N): BaseAction {

    abstract fun commitNavigatorAction()

}