#if (${PACKAGE_NAME} && ${PACKAGE_NAME} != "")
package ${PACKAGE_NAME}
#end

abstract class BaseNavigatorAction<out N>(protected val navigator: N): BaseAction {

    abstract fun commitNavigatorAction()

}